# T-0005: Configure Wandb for experiment tracking

**Status**: DONE
**Phase**: VERIFYING
**After**: —
**Before**: T-0007

## Context

The training script uses Weights & Biases (wandb) for experiment tracking. This requires authentication and optionally project configuration.

## Acceptance Criteria

- [x] wandb is authenticated (logged in or using API key)
- [x] Training can log to wandb without errors
- [ ] Alternative: offline mode configured if wandb not desired (N/A - using online mode)

## Verification Plan

### Tests to Create
| Test | Proves | Status |
|------|--------|--------|
| Manual: `wandb status` or `python -c "import wandb; wandb.login()"` | wandb configured | - |

### Regression Check
```
python -c "import wandb; print('wandb version:', wandb.__version__)"
```

### Manual Verification
- [ ] Check wandb can initialize a run (even in offline mode)

## Approach

(Filled during PLANNING phase)

## Implementation Notes

### PLANNING

- Wandb v0.23.1 installed
- No existing credentials found
- User chose online mode with API key authentication

### TESTING_RED

### IMPLEMENTING

- Ran `python -m wandb login <API_KEY>`
- Credentials stored in ~/.netrc
- Verified login as user: sverker-janson (sverker-janson-rise)

### TESTING_GREEN

### REFACTORING

### DEBUGGING

## Obstacles

## Evidence

```
$ python -c "import wandb; api = wandb.Api(); print(api.viewer)"
Logged in as: sverker-janson (sverker-janson-rise)

$ wandb.init(project='urm-test', mode='offline')
Wandb init test: OK
```

## Outcome

Wandb configured successfully. Logged in as sverker-janson. Credentials stored in ~/.netrc for use in training runs. Note: When syncing to ICE pod, ~/.netrc should be included or WANDB_API_KEY env var set.
